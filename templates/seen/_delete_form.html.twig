<form method="post" action="{{ path('app_seen_delete', {'id': seen.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ seen.id) }}">
    <button class="btn">Delete</button>
</form>
