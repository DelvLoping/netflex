<form method="post" action="{{ path('app_film_crew_delete', {'id': film_crew.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ film_crew.id) }}">
    <button class="btn">Delete</button>
</form>
